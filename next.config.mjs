/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export", // export the app as an static site
    basePath: "/"
    // images: {
    //     unoptimized: true, //  disable optimization for static site
    // },
    // reactStrictMode: true, //  enable strict mode for React
};

export default nextConfig;
